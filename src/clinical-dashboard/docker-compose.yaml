services:
  app:
    container_name: "application-server"
    build:
      context: .
      dockerfile: Dockerfile.minimal
    restart: always
    ports:
      - "8080:8080"
    environment:
      APP_ENV: "prod"
    networks:
      - app-network

networks:
  app-network:
    external: true
