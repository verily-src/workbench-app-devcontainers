{
  "id": "cohort-explorer",
  "version": "1.0.0",
  "name": "Cohort Explorer",
  "description": "No-code cohort exploration app for tabular sample data backed by Aurora PostgreSQL",
  "options": {
    "cloud": {
      "type": "string",
      "enum": ["gcp", "aws"],
      "default": "gcp",
      "description": "Cloud provider (gcp or aws)"
    },
    "login": {
      "type": "string",
      "description": "Whether to log in to workbench CLI",
      "proposals": ["true", "false"],
      "default": "true"
    }
  }
}
