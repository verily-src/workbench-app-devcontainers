services:
  app:
    container_name: "application-server"
    build:
      context: .
    restart: always
    volumes:
      - .:/workspace:cached
    ports:
      - 8080:8080
    networks:
      - app-network
    cap_add:
      - SYS_ADMIN
    devices:
      - /dev/fuse
    security_opt:
      - apparmor:unconfined
    environment:
      AURORA_RESOURCE_ID: "${AURORA_RESOURCE_ID:-}"
      TSV_PATH: "${TSV_PATH:-/root/workspaces/GTEx_demo_folder/GTEx_V8_sample_manifest_metadata.tsv}"

networks:
  app-network:
    external: true
