{
  "id": "example",
  "version": "1.0.0",
  "name": "example",
  "description": "Custom Workbench app: example (Image: quay.io/jupyter/base-notebook, Port: 8888, User: jovyan)",
  "options": {
    "cloud": {
      "type": "string",
      "enum": ["gcp", "aws"],
      "default": "gcp",
      "description": "Cloud provider (gcp or aws)"
    }
  }
}
