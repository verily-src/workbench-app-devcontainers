{
  "id": "gcp-data-characterizer-llm",
  "version": "1.0.0",
  "name": "GCP Data Characterizer with LLM",
  "description": "Connect to GCP (GCS, BigQuery), profile data with metrics and histograms, and talk to your data using an LLM (API key entered in app).",
  "options": {
    "cloud": {
      "type": "string",
      "enum": ["gcp", "aws"],
      "default": "gcp",
      "description": "Cloud provider (gcp or aws)"
    },
    "login": {
      "type": "string",
      "description": "Whether to log in to workbench CLI",
      "proposals": ["true", "false"],
      "default": "false"
    }
  }
}
