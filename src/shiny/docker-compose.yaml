version: '2.4'
services:
  app:
    container_name: "application-server"
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - BASE_IMAGE=rocker/shiny  
        # version tag of the rocker/shiny image.
        - VARIANT=4
    restart: always
    volumes:
      - .:/workspace:cached
      - work:/home/shiny:cached
    ports:
      - "3838:3838"
    environment:
      "DISABLE_AUTH": "true"
    networks:
      - app-network
    cap_add:
      - SYS_ADMIN
    devices:
      - /dev/fuse
    security_opt:
      - apparmor:unconfined
networks:
  app-network:
    external: true
volumes:
  work:
