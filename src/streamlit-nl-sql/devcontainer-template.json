{
  "id": "streamlit-nl-sql",
  "version": "1.0.0",
  "name": "Streamlit NL-to-SQL Dashboard",
  "description": "Natural language to SQL dashboard powered by Vertex AI Gemini. Query BigQuery datasets using plain English and visualize results interactively.",
  "options": {
    "cloud": {
      "type": "string",
      "enum": ["gcp", "aws"],
      "default": "gcp",
      "description": "Cloud provider (currently only GCP supported for BigQuery and Vertex AI)"
    },
    "login": {
      "type": "string",
      "description": "Whether to log in to Workbench CLI for advanced features",
      "proposals": ["true", "false"],
      "default": "false"
    }
  }
}
