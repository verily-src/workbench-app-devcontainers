{
  "name": "vscode app",
  "dockerComposeFile": "docker-compose.yaml",
  "service": "app",
  "shutdownAction": "none",
  // Open the sub-folder with the source code
  "workspaceFolder": "/workspace/vscode",
  "postCreateCommand": ["../post-startup.sh", "abc", "/config/workspace"],
  "remoteUser": "root"
}
